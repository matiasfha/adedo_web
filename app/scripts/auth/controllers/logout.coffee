'use strict'

angular.module('ADedoApp.auth')
	.controller 'LogoutCtrl', ['$rootScope','$scope','$location',($rootScope,$scope,$location) ->
		# $rootScope.CurrentUser.logout().then (response) ->
		# 	$location.path('/login')
]

